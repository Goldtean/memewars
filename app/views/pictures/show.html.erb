<div class="nav-offset container">
  <p id="notice"><%= notice %></p>

  <h2><%= @picture.category %></h2>
    <%= image_tag @picture.avatar.url(:medium) %>

  <div class="show actions">
    <%= link_to 'Edit', edit_picture_path(@picture) %>
    <%= link_to 'Remove', @picture, data: { confirm: 'Are you sure you want to lose a picture?' }, method: :delete, class: 'destroy' %>
    <%= link_to 'Back to all my pictures', pictures_path %>
  </div>
</div>