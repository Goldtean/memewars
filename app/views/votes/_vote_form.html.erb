<%=form_for vote, remote: true, authenticity_token: true do |f|%>
  <%= f.label :vote%>:
  <%= f.hidden_field :meme_id, value: meme.id %>
  <%= f.submit "Vote", class: "btn btn-primary", data: {send: "vote"}%>
<%end%>